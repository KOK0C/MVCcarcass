
<div class="col-sm-3 offset-sm-1 blog-sidebar">
    <div class="sidebar-module sidebar-module-inset">
        <h4>Календарь</h4>
        <table id="calendar2">
            <thead>
            <tr><td>‹<td colspan="5"><td>›
            <tr><td>Пн<td>Вт<td>Ср<td>Чт<td>Пт<td>Сб<td>Вс
            <tbody>
        </table>
    </div>
    <div class="sidebar-module text-center">
        <h4>Производители</h4>
        <div class="btn-container">
            <button class="slider-btn" id="upButton"><i class="fa fa-caret-square-o-up" aria-hidden="true"></i></button>
        </div>
        <aside>
            <button class="slider-btn" id="moreBrands" data-toggle="modal" data-target="#allBrand">
                <i class="fa fa-caret-square-o-left" aria-hidden="true"></i>
            </button>
            <div class="slider">
                <ol class="list-unstyled">
                    <?php if (! empty($brands)): ?>
                        <?php foreach ($brands as $brand): ?>
                            <li class="sidebarItem <?php if (isset(explode('/', $_SERVER['REQUEST_URI'])[2])
                                && explode('/', $_SERVER['REQUEST_URI'])[2] === str_replace(' ', '-', mb_strtolower($brand->name))):
                                print 'active-SideBar';
                            endif;?>">
                                <a href="/mark/<?= htmlentities(str_replace(' ', '-', mb_strtolower($brand->name))); ?>" class="sidebarLink">
                                    <div class="sidebarPic">
                                        <img src="<?= htmlentities($brand->getLogo()); ?>" alt="">
                                    </div>
                                    <div class="sidebarText">
                                        <span><?= htmlentities(mb_strtoupper($brand->name)); ?></span>
                                    </div>
                                </a>
                            </li>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </ol>
            </div>
        </aside>
        <div class="btn-container">
            <button class="slider-btn" id="downButton"><i class="fa fa-caret-square-o-down" aria-hidden="true"></i></button>
        </div>
    </div>

</div><!-- /.blog-sidebar -->

<div id="forModal">
    <?php if (! empty($brands)): ?>
        <div class="modal fade" id="allBrand" tabindex="-1" role="dialog" data-backdrop="static">
            <div class="modal-dialog modal-lg" role="dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title">Марки авто</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        <ul class="modal-brand">
                            <?php foreach ($brands as $brand): ?>
                                <li class="sidebarItem <?php if (isset(explode('/', $_SERVER['REQUEST_URI'])[2])
                                    && explode('/', $_SERVER['REQUEST_URI'])[2] === str_replace(' ', '-', mb_strtolower($brand->name))):
                                    print 'active-SideBar';
                                endif;?>">
                                    <a href="/mark/<?= htmlentities(str_replace(' ', '-', mb_strtolower($brand->name))); ?>" class="sidebarLink">
                                        <div class="sidebarPic">
                                            <img src="<?= htmlentities($brand->getLogo()); ?>" alt="">
                                        </div>
                                        <div class="sidebarText">
                                            <span><?= htmlentities(mb_strtoupper($brand->name)); ?></span>
                                        </div>
                                    </a>
                                </li>
                            <?php endforeach; ?>
                        </ul>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Закрыть</button>
                    </div>
                </div>
            </div>
        </div>
    <?php endif; ?>
</div>